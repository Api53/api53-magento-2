require(
    [
        'jquery',
        'mage/translate',
    ],
    function ($) {
      //add your code
	  $(document).ready(function(e) {
		 $('#api53_settings_sku_amount').prop('readonly', true);   
	  });
    } 
);